# Tech Radar — SOC AI Agent v10

| Категория | Adopt (внедрено) | Trial (пилотировать) | Assess (изучить) | Hold (пока не нужно) |
|-----------|--------------------|-----------------------|-------------------|------------------------|
| **Архитектура / Паттерны** | • Static typing (`mypy .`) • Централизованный exception handler • Graceful shutdown + сигналы • `asyncio.wait_for` — таймауты • Иерархия исключений (`SOCAgentError`) • Circuit Breaker • Rate Limiter • Prompt Injection (middleware) • MCP-клиент (JSON-RPC, aiohttp) • Мульти-агентная оркестрация • Approval workflow (Human-in-the-Loop) • Батчевый инференс (batch 64) • ONNX INT8 квантование • Security-by-Design (audit-log, dry-run, deny-by-default) • Model Registry + Fallback Routing | • Distributed tracing (OpenTelemetry) • Feature flags для деплоя агентов • Active Learning (модель запрашивает разметку) • LoRA fine-tune | • Chaos‑инжиниринг отказов MCP/LLM | • Ручное тестирование промптов (автоматическое уже есть) |
| **AI / ML** | • Ollama + Llama3-8B (CPU, 4-bit, объяснение алертов) • all-MiniLM-L6-v2 (embedding, Qdrant) • LogBERT (ONNX INT8, batch inference) • Drain3 (aiohttp, парсинг логов) • Имитация атак (hydra, nmap) для сбора TP • Legitimate baseline (7–14 дней норм. трафика) | • DistilBERT (триаж алертов) • SecureBERT → Sigma (ручная валидация) • LogHub / открытые датасеты для пре-тренировки | • LogPPT (замена LogBERT) • Ежеквартальное дообучение • Lakera Guard (детекция инъекций) | • Мульти-модельные ансамбли • vLLM (до фазы 4) • Celery (воркеры не нужны, asyncio достаточно) |
| **Compliance / Security** | • Audit-log всех tool_calls с хэшированием • Dry-run mode для ResponderAgent • Deny-by-default для новых инструментов • Pydantic strict (валидация MCP-схем) • YAML-библиотека: 10 правил ГОСТ 57580 + CIS L1 | • OpenSCAP / Lynis (MCP-инструмент) • Compliance Score в Grafana | • Полный профиль ГОСТ 57580 • ФСТЭК / ЦБ РФ • OWASP Agentic AI Top 10 (полный аудит) • Red teaming | — |
| **Инфраструктура** | • Redis Streams (шина логов) • Qdrant (векторный поиск) • Prometheus + Grafana (метрики) • Docker | • Kafka/Redpanda (замена Redis при EPS >500 или qual. триггерах) • Kubernetes + probes | • API Gateway для MCP • Service Mesh (istio/linkerd) • Falco + Talon (только если есть K8s) | • Локальный запуск без оркестрации (уже работает) |
| **Инструменты разработки** | • `ruff` (линтер/форматер) • `pytest` + `pytest-asyncio` + `aioresponses` • `structlog` • GitHub Actions (CI: ruff → mypy → pytest) • `pydantic-settings` + `SecretStr` • `tenacity` (retries) | • Coverage (`pytest-cov`) • `bandit` (безопасность) • `pre-commit` хуки • MLflow (эксперименты) | • LangSmith / ML‑flow (продакшен) • Trivy для Docker | • Самописный логгер (уже `structlog`) • Celery |
| **Платформы / Интеграции** | • Wazuh (лог-менеджмент) • Grafana (дашборды, кнопки TP/FP) • Orchestrator (Workflow, Python) • MISP API / AlienVault OTX (лёгкое обогащение) • Локализация: RU в UI, логах, документации | • n8n (workflow с внешними сервисами) • TheHive / IVRE (кейс-менеджмент) • Telegram бот (уведомления) • Партнёрства: 2–3 MOU с интеграторами | • OpenCTI (TIP, фаза 3) • LangGraph (агентная оркестрация, фаза 4) • Case studies на Хабре, SOC Forum | • Замена `aiohttp` на `httpx` (не нужна) • Kibana (для TP/FP кнопок — Grafana проще) |
